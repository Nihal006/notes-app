sequenceDiagram
    participant browser
    participant server

    browser->>server: GET /spa
    activate server
    server-->>browser: HTML document
    deactivate server

    browser->>server: GET /spa.js
    browser->>server: GET /main.css

    Note right of browser: Browser starts executing SPA JavaScript

    browser->>server: GET /data.json
    activate server
    server-->>browser: JSON notes data
    deactivate server

    Note right of browser: JS renders notes\nwithout reloading the page
